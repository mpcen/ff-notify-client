import * as React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const SignIn = () => {
    return <Text style={{ fontSize: 48 }}>Signin</Text>;
};

const styles = StyleSheet.create({});

export { SignIn };
